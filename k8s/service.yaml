apiVersion: v1
kind: Service
metadata:
  name: depositlk-service
spec:
  type: NodePort
  selector:
    app: depositlk
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080